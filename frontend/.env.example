# Backend API URL (server-side proxy, not exposed to browser)
BACKEND_API_URL=http://localhost:8000

# API key for protected backend routes (server-side only, never in browser bundle)
BACKEND_API_KEY=
