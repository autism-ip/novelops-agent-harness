# Backend API URL (baked into client bundle via NEXT_PUBLIC_*)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# API key for protected backend routes (baked into client bundle via NEXT_PUBLIC_*)
# Leave empty for public routes only
NEXT_PUBLIC_API_KEY=
